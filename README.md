# Covid Tracker React App

Simple Covid Tracker. This was build in React and uses: Axios to fetch data from a Covid API. Data is presented using custom SCSS and react-chartjs.

## Available Scripts

In the project directory, you can run:

### `npm start`

### `npm test`

### `npm run build`
